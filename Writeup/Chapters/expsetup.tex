% Chapter Template

\chapter{Experimental Setup} % Main chapter title

\label{Chapter3} % Change X to a consecutive number; for referencing this chapter elsewhere, use \ref{ChapterX}

This chapter is intended to describe the details of the experimental setup used in this project. It contains a description of the wave model used to generate the forecasts, as well as an overview of the background state, the observations used and the errors present in these observations. Finally a description is given of the twin experiment method used in this project, as well as the data assimilation setup.

%----------------------------------------------------------------------------------------
%	SECTION 1
%----------------------------------------------------------------------------------------

\section{Model Description and Domain}
\label{sec:model}

Waves are modelled by Fugro using WW3 \citep{WW3DG2019}, which uses as its base spectrum a wavenumber-direction spectrum $F(k,\theta)$, rather than the frequency-direction spectrum described in section \ref{sec:wavespectra}. The spectrum is discretised using 29 sampled wavenumbers $k$ and 24 angles $\theta$. The wavenumber-direction spectrum is related to the frequency-direction spectrum by the Jacobian transform

\[
F(\sigma,\theta) = \frac{\partial k}{\partial \sigma}F(k,\theta) = \frac{1}{c_g}F(k,\theta)
\]

Wave propagation is then described by the equation

\begin{equation}
    \frac{D}{Dt}N(k,\theta) = \frac S \sigma
\end{equation}

Where $N(k,\theta) = F(k,\theta)/\sigma$ as before and $S$ describes the net effect of sources and sinks for the spectrum $F(k,\theta)$.

In deep water, $S$ consists of three main parts: an atmospheric interaction term $S_{in}$, a nonlinear wave-wave interaction term $S_{nl}$ and a dissipation term $S_{ds}$.

$S_{in}$ is dominated by an exponential wind-wave growth term, so $S_{in}$ is equal to some coefficient multiplied by $\sigma N(k,\theta)$. This leads to a feedback which is usually positive but can be negative in the case of swell\remove{,} and represents the main source of wave energy.

$S_{ds}$ is generally dominated by wave breaking, and it represents a sink of wave energy. Similar to $S_{in}$, it is equal to a negative coefficient multiplied by $\sigma N(k,\theta)$. The balance between $S_{in}$ and $S_{ds}$ determines the overall growth or decay of wave energy.

$S_{nl}$ represents interactions between waves due to nonlinear processes, which results in energy transfer between waves of different frequencies. \highlight{(maybe expand -}  \citealp{Janssen2004})

In shallow water, processes such as wave-bottom interactions, wave breaking and reflection off shorelines become important, so additional source terms are included to describe this.

From the forecasted spectra, other variables such as wave height, period and mean direction are derived. The three variables we will be focusing on in this study are significant wave height Hs, and two measures of the dominant wave period \comment{Tm01 and Tm02}{Should be formatted as symbols throughout.}. These are related to the spectrum via the spectral moments (equations \ref{eqn:moments}, \ref{eqn:mainvars}).

The reason for assimilating both Tm01 and Tm02 is that they are defined from different spectral moments, so assimilating both will provide more information about the spectrum than either one alone, which, \add{theoretically,} should lead to better DA results.

As forcing, WW3 uses just the 10 metre wind, and this comes from \replace{the GEFS}{the National Oceanic and Atmospheric Administration's (NOAA) National Global ensemble forecast system (GEFS)} \comment{ensemble}{add reference}. The GEFS forecast contains one control run plus 20 perturbed ensemble members. \comment{(What is the relation between GEFS members and WAVEWATCH members?)}{Do I understand correctly that there are more WW3 members than GEFS members?}

The domain in which forecasts are produced lies between $15.0 - 33.0\degrees$N and $79.1 - 100\degrees$W in the \replace{g}{G}ulf of Mexico. Fugro use a nested domain to produce the forecasts, running WW3 on a $0.5\degrees$ resolution global rectangular grid, and two higher resolution nested grids covering the region of interest. There is a $0.1\degrees$ resolution grid spanning the region described above\remove{,} and a $0.05\degrees$ grid spanning from $18 - 22.5\degrees$N and $90.5 - 96.5\degrees$W. These domains are shown on a map in figure \ref{fig:domain}.

\begin{figure}[h]
\centering
\includegraphics[width = .8\textwidth]{Figures/modelDomain}
\caption{Model domains shown on map.}
\label{fig:domain}
\end{figure}

Forecasts are produced at 12Z each day, and run up to seven days ahead. \replace{We have}{There are} seven of these daily runs available\remove{,} with start dates spanning from 06/03/2022 to 12/03/2022. A schematic of this process is shown in \comment{figure}{Could you add arrows indicating lead time to the figure?} \ref{fig:fugrosetup}.  As we do not have access to the model code in this project, we will treat each of these runs as \replace{one 'timestep' of the DA system}{as a separate DA problem to optimised. In particular, we do not employ cycling: correction from 7-day window is not used in the next 7-day window}. Further description of how this works can be found in section \ref{sec:DAsetup}.

\begin{figure}[h]
\centering
\includegraphics[width = .9\textwidth]{Figures/fugrosetup}
\caption{Schematic of the forecasting setup used by Fugro. A new 7-day 31-member ensemble forecast is produced at 12Z each day.}
\label{fig:fugrosetup}
\end{figure}

%----------------------------------------------------------------------------------------
%	SECTION 2
%----------------------------------------------------------------------------------------

\section{Background State and Climatology}
\label{sec:background}

In this section we present a general overview of the background state and climatology in the domain. This will mostly come from the forecast data which spans a period from 06/03/2022 to 19/03/2022. Since the available observations span a longer period of roughly six months, observations will also be used to identify broader climatological features at the buoy location. 

\subsection{Model Background State}

Figure \ref{fig:hdirtimemean} shows the significant wave height and direction averaged over the first six days and last six days of the forecast period. Hs is mostly between 1m and 2m in the central gulf, with lower wave heights of < 1m in sheltered coastal areas such as the western side of Florida and the Yucatan peninsula. The central part of the domain has largely east-southeasterly waves over both periods, which diverge after they pass between Yucatan and Cuba. The two periods are separated by the passage of strong cold front (figure \ref{fig:wavesandchart}) and there is a slight shift to more easterly waves after the front has passed. This change is likely related to the wind backing associated with the front. The southern part of the \add{Guld of Mexico} GoM has slightly higher waves, around 1 - 1.5m, in the six day period after the passage of the front. \highlight{(check other climatologies)}.

\begin{figure}
\centering
\begin{subfigure}{.45\textwidth}
\centering
\includegraphics[width = \textwidth]{Figures/hdir6-12}
\end{subfigure}
\begin{subfigure}{.45\textwidth}
\centering
\includegraphics[width = \textwidth]{Figures/hdir13-19}
\end{subfigure}
\caption{Left: \replace{Hs}{Significant wave height} (colours) and wave direction (arrows) averaged between 12Z 06/03/2022 and 12Z 12/03/2022. Right: The same averaged between 12Z 13/03/2022 and 12Z 19/03/2022}
\label{fig:hdirtimemean}
\end{figure}

There is a large peak in \replace{Hs}{significant wave height} on the 12/03/2022, related to a cold front passing through the region. A map of Hs in the GoM at 18:00 on 06/03/2022 is shown in figure \ref{fig:wavesandchart}, along with a surface chart from the same time. The cold front is visible on the chart, and high waves are present in a large region behind the front. This is in line with \citet{Appendini2014}, a study into the wave climatology of the GoM. In that paper, cold fronts are identified as a common driver of high waves between October and April, with the most intense peaks occuring between December and March.

\begin{figure}
\centering
\begin{subfigure}{.45\textwidth}
\centering
\includegraphics[width = \textwidth]{Figures/0312waves}
\end{subfigure}
\hfill
\begin{subfigure}{.45\textwidth}
\centering
\includegraphics[width = \textwidth]{Figures/0312chart}
\end{subfigure}
\caption{Left: Hs (colours) and direction (arrows) in the GoM at 18Z on 12/03/2022. Right: Surface analysis chart valid for 18Z on 12/03/2022, taken from NOAA WPC \comment{surface analysis archive}{add reference}.}
\label{fig:wavesandchart}
\end{figure}

Figure \ref{fig:t01maps} shows Tm01 at 18Z 12/03/2022 and averaged over the preceding 48 hours. The high waves associated with the cold front at 18Z have a period of $\sim$ 8s\replace{, which}{This} is higher than the period in the rest of the GoM at that time and also higher than the waves across the entire GoM in the 48 hours prior. This suggests these waves are \comment{swell waves}{To be swell waves they have to travel in from a different location. Is that the case here, or are we seeing just the effects of the front creating large, long waves behind the front?}.

\begin{figure}
\centering
\includegraphics[width = \textwidth]{Figures/backgroundt01map}
\caption{Left: Tm01 at 18Z on 12/03/2022. Right: Tm01 averaged over the period from 12Z 10/03/2022 to 12Z 12/03/2022}
\label{fig:t01maps}
\end{figure}

Figure \ref{fig:hsensseries} shows a time series of \comment{Hs}{Should be formatted as symbol throughout.} ensemble mean and \add{ensemble} variance at the buoy location from the forecast starting 12Z 10/03/2022. We see the peak on the 12th as seen in figure \ref{fig:wavesandchart}. We also see that the variance is a strong function of the mean. The variance in Hs peaks just before the peak in the mean, and drops back to a lower value as the mean reaches its peak. This is notable since the diagonal elements of our forecast error covariance matrix $\mat{P}^f$ are simply the ensemble variances at each grid point and time step.\replace{, so ensemble variance is a good measure of the magnitude of the background error. This means that the background error in Hs is highest just before the peak in Hs.}{This indicates that forecast errors are not completely relative to the field magnitudes. The largest forecast errors actually occur in the hours preceding the maximum in significant wave height.}

Figure \ref{fig:t01ensseries} shows the same as  \ref{fig:hsensseries} but for Tm01. Again we see a peak in the variance just before the peak in the mean. In this case peak in Tm01 is preceded by a \comment{dip}{Is it known what physical process causes this dip?}, and this dip is also associated with a peak in the ensemble variance. This means that the background error in Tm01 is high for a period of $\sim 24$ hours, as opposed to $\sim 12$ hours for Hs.

\comment{}{Could you add a time series figure with atmospheric pressure and wind speed. It would be interesting to see whether the max. errors coincide with the passing of the front and whether the peaks are associated with swell waves passing or maximum local generation.}

\begin{figure}
\centering
\includegraphics[width = .8\textwidth]{Figures/buoyhsvar}
\caption{Time series of forecast Hs ensemble mean and variance at the buoy location from the 10/03/2022 model run.}
\label{fig:hsensseries}
\end{figure}

\begin{figure}
\centering
\includegraphics[width = .8\textwidth]{Figures/buoyt01var}
\caption{Time series of forecast Tm01 ensemble mean and variance at the buoy location from the 10/03/2022 model run.}
\label{fig:t01ensseries}
\end{figure}

\highlight{correct upto this point}

\subsection{Climatology at Buoy Location}

Figure \ref{fig:buoyseries} shows a timeseries of Hs and Tm01 as measured by the buoy. We that outside of specific peaks, Hs is generally < 1m. This is consistent with the model background state which showed Hs in the buoy region to be < 1m for much of the forecast period. The peaks generally consist of a sharp increase in Hs followed by a more gradual decrease down to the baseline level. This is consistent with the slightly higher waves seen in the model background state in the buoy region after the passage of the front. As seen in the background state, the peaks in Hs are associated with peaks in Tm01. This suggests that the high waves seen at the buoy are travelling swell waves and are not generated in the vicinity of the buoy.

\begin{figure}
\centering
\includegraphics[width = \textwidth]{Figures/buoyseries}
\caption{Timeseries of observed Hs (top) and Tm01 (bottom) over a three-month period from 01/12/2021 to 01/03/2022}
\label{fig:buoyseries}
\end{figure}

Figure \ref{fig:obsdensityplot} shows a density plot of observed Hs and wave direction at the buoy location. There is a clear peak in frequency corresponding to waves of height $\sim$ 0.5m coming from a northeasterly direction. Some higher waves (up to $\sim$ 5m) are present coming from a north-northwesterly direction. This lines up with the direction of waves produced by the cold front on 12/03/2022, suggesting that the waves produced by this kind of event consistently reach the buoy from a north-northwesterly direction. Since the buoy is near the coast in the southern part of the gulf, it is sheltered from the south so there are virtually no waves coming from this direction.

\begin{figure}
\centering
\includegraphics[width = .6\textwidth]{Figures/obsdensityplot}
\caption{Density plot of Hs (radial axis) and wave direction at the buoy location.}
\label{fig:obsdensityplot}
\end{figure}

%----------------------------------------------------------------------------------------
%	SECTION 3
%----------------------------------------------------------------------------------------

\section{ADCP Observations}
\label{sec:observations}

Our observations come from an acoustic doppler current profiler (ACDP) buoy, which estimates the spectrum by measuring the wave induced velocity and surface height variations. According to \cite{Vallis2017}, the velocity and surface height anomaly induced by a simple wave with wave vector $\mathbf{k} = \left(k_x,k_y\right)$, frequency $\sigma$ and amplitude $a$ is

\begin{align*}
	u &= a \frac {k_x} {\sigma} g \exp{i(\mathbf{k}\cdot\mathbf{x} - \sigma t)} \cosh k(z + D) \\
	v &= a \frac {k_y} {\sigma} g \exp{i(\mathbf{k}\cdot\mathbf{x} - \sigma t)} \cosh k(z + D) \\
	w &= -i a \frac k \sigma g \exp{i(\mathbf{k}\cdot\mathbf{x} - \sigma t)} \sinh k(z + D) \\
	\eta &= a \exp{i(\mathbf{k}\cdot\mathbf{x} - \sigma t)}
\end{align*}

where only the real part of each expression should be taken. Note that the horizontal velocity $(u,v)$ is always in the same direction as the wave vector $\mathbf{k}$. From the ADCP measurements, a timeseries of wave induced energy per unit area $E = \int_{-D}^\eta \frac 1 2 \rho_0 \norm{\mathbf{u}}^2 dz + \frac 1 2 \rho_0 \eta^2$ can be calculated. This is equivalent/a good approximation to the wave energy in equation \ref{eqn:waveenergy} (source) so a Fourier transform of $E$ gives the wave spectrum integrated across all directions $E(\sigma) = \int F(\sigma,\theta) d\theta$ (how is directional distribution estimated?)

Other variables are then deduced from this spectrum, including Hs, Tm01 and Tm02 as described above.

We have observations available over a roughly 6 month period spanning from October 2021 to April 2022, which includes the period for which forecasts are available. A timeseries of the observed significant wave height from the forecast period is included in figure \ref{fig:obshsseries}. The peak in wave height late on the 12th of March is clearly visible on the plot.

\begin{figure}
\centering
\includegraphics[width = .8\textwidth]{Figures/obshsseries}
\caption{Observed timeseries of significant wave height from ADCP buoy}
\label{fig:obshsseries}
\end{figure}

(MORE ON OBSERVATIONS - TAKE CLIMATOLOGY SECTION AND EXPAND)

%----------------------------------------------------------------------------------------
%	SECTION 4
%----------------------------------------------------------------------------------------

\section{Twin Experiment}
\label{sec:twinexp}

In this study, we will not be using real observations in our assimilation but setting aside one ensemble member to represent the `truth' and generating fictional observations based on this forecast data. This is known as a twin experiment, and has several advantages over assimilating real observations in this context:

\begin{itemize}
\item The `truth' is available away from the location of observations, so forecast error can be calculated over the whole model domain. This allows the effectiveness of DA to improve forecasts away from observation locations to be evaluated
\end{itemize}

The observations are generated by sampling values from the truth at predefined observation locations and times. Here we sample hourly values from the nearest gridpoint to the buoy to match the real observations that are available. To represent observation error, some random noise is added to the sampled values. This noise is drawn from a normal distribution with standard deviation equal to the error standard deviation derived in section \ref{sec:observations}

A downside of using the twin experiment method in this project is its inability to correct biases in the model. Figure \ref{fig:hsominusb} shows a possible bias in the forecasted values of Hs, which would be desirable to try to correct with DA. Since the data that we assimilate in this project is also from model output, it will be subject to all the same biases as the forecast data so it is impossible for the DA to correct these biases.

%----------------------------------------------------------------------------------------
%	SECTION 5
%----------------------------------------------------------------------------------------

\section{Data Assimilation Setup}
\label{sec:DAsetup}

To perform the data assimiliation we use pyPDAF \citep{Chen2024}, which is a python interface for the fortran-based Parallel Data Assimilation Framework (PDAF). We will use the ETKF method which comes builtin with PDAF. As we are not able to run the WAVEWATCH III model ourselves, at each analysis step we will correct one 7-day forecast window all at once, and then the forecast step will consist of selecting data from the next 7-day window. In this way the time within the 7-day window is treated like another spatial coordinate by the DA system. A schematic of this process is included in figure \ref{fig:ETKFschematic}

At each analysis step, we will assimilate observations from the first three days of the forecast window. This allows us to investigate errors at times after the assimilation window and assess the capability of DA to improve the forecast at future time when observations are unavailable.

\begin{figure}
\centering
\includegraphics[width = \textwidth]{Figures/ETKFschematic}
\caption{Schematic of the forecast and analysis steps from the ETKF scheme employed in this project.}
\label{fig:ETKFschematic}
\end{figure}
